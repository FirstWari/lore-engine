# lore-engine - optional settings (copy to .env). No API keys: everything runs locally.

# Where downloaded videos and subtitles land (results go to --out, default results/)
# LORE_WORKSPACE_DIR=downloads

# Netscape-format cookies file for Coursera (export from your logged-in browser)
# COURSERA_COOKIE_FILE=www.coursera.org_cookies.txt
